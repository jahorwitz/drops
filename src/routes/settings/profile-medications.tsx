import { Form } from "../../components";

//create fake data to initally load into component

export const ProfileMedications: React.FC = () => {
  return (
    <Form.AddMoreSection buttonText="+ Add medication">
      <p>data</p>
    </Form.AddMoreSection>
  );
};
